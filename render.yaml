services:
  - type: web
    name: recipe-app-api
    runtime: python
    rootDir: backend
    region: singapore
    plan: free
    buildCommand: uv sync --frozen --no-dev
    startCommand: >
      sh -c 'uv run --frozen --no-dev alembic -c alembic.ini upgrade head &&
      uv run --frozen --no-dev uvicorn app.main:app --host 0.0.0.0 --port "$PORT"'
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: recipe-app-db
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: FRONTEND_ORIGINS
        sync: false

databases:
  - name: recipe-app-db
    region: singapore
    databaseName: recipe_app
    user: recipe_app
    plan: free
    postgresMajorVersion: "16"
    ipAllowList: []
